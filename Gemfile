# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in actionset.gemspec
gemspec

gem 'activeset', path: '~/gems/activeset'

group :test do
  gem 'actionpack'
  gem 'activerecord'
  gem 'rspec-rails'
  gem 'sqlite3'
end
